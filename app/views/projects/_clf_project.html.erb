  <tr class="<%= cycle("odd", "even") %>">
	<td class="name" style="padding-left: <%= project.level * 0.8 %>em;"><p><%= link_to(h(project.name), :controller => 'projects', :action => 'show', :id => project) %></p></td>
	<td>
      <%= textilizable(truncate_words(project.description, 50), :project => project)%>
    </td>
  </tr>
